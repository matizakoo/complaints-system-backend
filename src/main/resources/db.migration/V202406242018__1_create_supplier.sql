CREATE TABLE IF NOT EXISTS `supplier` (
                                      `id` BIGINT AUTO_INCREMENT,
                                      `name` VARCHAR(255) NOT NULL,
                                      PRIMARY KEY (`id`)
);
