spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tanielazienki
    username: root
    password: password
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect